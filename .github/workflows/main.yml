name: CI

on:
  pull_request:
  push:
    branches:
      - development
      - master

env:
  VERSION_1: ${{ (date +'%Y-%m-%dT%H-%M-%S') }}
  VERSION_2: ${{ date +'%Y-%m-%dT%H-%M-%S' }}
  VERSION_3: ${{ (date +'%Y-%m-%dT%H-%M-%S') }}

jobs:
  # deploy-to-production:
  #   if: endsWith(github.ref, 'master') == true
  #   needs: [test, package]
  #   runs-on: ubuntu-20.04
  #   steps:
  #     - run: echo "${VERSION}"

  # package:
  #   if: endsWith(github.ref, 'master') == true
  #   needs: [test]
  #   runs-on: ubuntu-20.04
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v2
  #     - name: Create release
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       uses: actions/create-release@v1
  #       with:
  #         draft: false
  #         prerelease: false
  #         release_name: Release ${{ env.VERSION }}
  #         tag_name: ${{ env.VERSION }}

  test:
    runs-on: ubuntu-20.04
    steps:
      - run: echo ${VERSION_1}
      - run: echo ${VERSION_2}
      - run: echo ${VERSION_3}
      # - name: Checkout code
      #   uses: actions/checkout@v2
      # - name: Use node
      #   uses: actions/setup-node@v1
      #   with:
      #     node-version: 12.x
      # - run: npm install
      # - run: npm test
